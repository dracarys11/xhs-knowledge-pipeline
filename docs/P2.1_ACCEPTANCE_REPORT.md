# P2.1 Exporter Acceptance Report

## Status

**PENDING REAL-DATA ACCEPTANCE**

The P2.1 implementation is frozen for acceptance. This report must not claim
P2.1 complete until every gate below has recorded evidence.

## Automated Evidence

| Check | Result | Evidence |
| --- | --- | --- |
| Full regression suite | PASS | `125 passed` |
| Exporter suite | PASS | `34 passed` |
| SQLite read-only access | PASS | Exporter test coverage |
| P1 isolation guards | PASS | Exporter test coverage |
| Vault symlink/path boundary guards | PASS | Exporter test coverage |
| Ownership-limited stale cleanup | PASS | Exporter test coverage |

## Real-Data Acceptance Evidence

| Gate | Status | Evidence |
| --- | --- | --- |
| Real export completed | PENDING | No `Vault/export_manifest.json` recorded at checkpoint time. |
| Export counts reconciled | PENDING | Record tracked, COMPLETE candidates, evaluated, exported, skipped, and failed from the real manifest. |
| P1 database unchanged | PENDING | Compare pre/post database hash and logical state counts. |
| P1 `data/` unchanged | PENDING | Compare pre/post artifact inventory and hashes. |
| Vault filesystem reconciled | PENDING | Compare manifest counts with generated notes and assets. |
| Obsidian manual verification | PENDING | Open the generated Vault; verify notes, image paths, source links, and observed media types. |

## Media Evidence Boundary

Video path handling is implemented and fixture-tested. Real-video export is
**not validated** unless a real exported dataset contains video and that result
is recorded here. It must not be inferred from image-only media evidence.

## Completion Rule

Mark P2.1 **FROZEN** only after the pending gates have concrete, reproducible
evidence. The completion statement must describe local export only and retain:

```text
Remote coverage proof: UNPROVEN
```
